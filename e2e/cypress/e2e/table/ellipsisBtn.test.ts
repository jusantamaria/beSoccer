import Timeout from '../../support/enums/timeouts'

describe('Verify Frontend components', () => {
    beforeEach(() => {
        cy.visit('/');
        cy.login();
    })
    it('User can set favourites from ellipsis button', () => {
        
    });
});
